graph=trips

## writes
graph.partition.count=16
write.vertex.count=1000000
write.seed=12345

# write threads, per vm in spark
write.thread.count=16

## Reads
read.thread.count=64
read.operation.count=100000
